on: 
  workflow_dispatch:
jobs:
  examble:
    runs-on: ubuntu-latest
    steps:
      - name: Set API KEY
        run: echo "API_KEY=1234" >> $GITHUB_ENV
          
      - name: Use API KEY
        run: echo "using API KEY $API_KEY"
        
          
